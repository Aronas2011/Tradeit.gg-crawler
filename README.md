# Tradeit.gg-crawler
tradeit.gg website crawlerusing python library Scrapy

Same as my other website crawler project, but instead of the website having static details hardcoded into the HTML code, i had to intercept the api and then parse the json
